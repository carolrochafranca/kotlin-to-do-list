# Plano de implementação

O aplicativo separa persistência (Room), acesso a dados (Repository), estado de tela (ViewModel) e interface Compose. A entrega é evoluída em commits pequenos para tornar cada decisão verificável.
